# DEPRECATED — superseded in v1.0.1
#
# The example print-client service has been merged into the root
# docker-compose.yml using Docker Compose profiles.
#
# To run the example client, use:
#   docker compose --profile example up
#
# This file is kept only for reference and will be removed in a future version.
# See the root docker-compose.yml for the current configuration.
